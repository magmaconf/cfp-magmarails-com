- content_for :header_title do
  = t('profiles.profile')

.eight-columns
  = form_for @profile, url: profile_path do |form|
    = render 'form', form: form

    .btn-aro
      = form.submit t('profiles.update'), class: 'aro'

.one-column
