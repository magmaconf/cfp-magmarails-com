- content_for :header_title do
  = t('devise.profiles.profile')

.eight-columns
  = form_for @profile, :url => profile_path do |form|
    = render 'form', :form => form

    .btn-aro
      = form.submit class: 'aro', value: 'Update'
.one-colum
